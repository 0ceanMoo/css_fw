h3._lefted カード
.card-box
  ._Card._w-300px
    h4 カードの説明
    pre._code
      .
        .myCard {
          @extend ._Card-base
          .image
            // 設定
          .title
            // 設定
          .content
            // 設定
        }
    p カードデザインについてはいくらデザイン作っても、実際に使う時どうせそのまま使えないので「._Card-base」という基本設定を持つクラスを@extendして、細かい部分は自前でカスタマイズすること。
style
  .
    .card-box {
    }
